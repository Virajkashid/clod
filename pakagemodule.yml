---
-
   name: example plybook debian setup
   host: all
   become: yes
   vars: package apache 

   task: installing packege apache
      name: apache
      action: apt instalation 
      state: latest 
... 